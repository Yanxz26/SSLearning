<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.app.mapper.UserMapper">

    <select id="selectById" resultType="com.example.app.entity.User">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="selectByOpenId" resultType="com.example.app.entity.User">
        SELECT * FROM users WHERE open_id = #{openId}
    </select>

    <insert id="insert" parameterType="com.example.app.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (open_id, nick_name, avatar_url, phone, email, week_start_day, created_at, updated_at)
        VALUES (#{openId}, #{nickName}, #{avatarUrl}, #{phone}, #{email}, #{weekStartDay}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="com.example.app.entity.User">
        UPDATE users 
        SET nick_name = #{nickName}, avatar_url = #{avatarUrl}, phone = #{phone}, 
            email = #{email}, week_start_day = #{weekStartDay}, updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM users WHERE id = #{id}
    </delete>

</mapper>
